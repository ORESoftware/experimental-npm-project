name: shared_auth_client
description: Thin typed Dart client for the shared-auth HTTP API.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  http: ">=1.2.0 <2.0.0"

dev_dependencies:
  test: ">=1.26.3 <2.0.0"
